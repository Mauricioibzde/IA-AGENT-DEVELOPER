# Image-to-Code

> Planejado na Fase 3+ (visão sob demanda via Ollama).

Fluxo: upload mockup → VisionModelProvider (load) → spec estruturada → code model → run → capture → Visual Engine compare → unload vision.

A ausência de modelo de visão **não** bloqueia URL×URL nem imagem×imagem.
